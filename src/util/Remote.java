package util;

/**
 * This interface indicates whether this object should be returned as value or
 * reference.
 * 
 * If it implements remote reference, it should be returned as
 * RemoteObjectReference.
 * 
 * @author Jerry Sun
 * 
 */
public interface Remote {
	
}
